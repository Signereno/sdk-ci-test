language: csharp
solution: DoggoApp.sln
mono: none
dotnet: 2.1.300

install:
  - dotnet restore src/
  - dotnet build src/
  
script:
  - dotnet pack -c Release
  - dotnet pack -c Release --version-suffix $TRAVIS_JOB_ID
  
deploy:
  - provider: releases
    api_key: $GITHUB_OAUTH_TOKEN
    on:
      tags: true
